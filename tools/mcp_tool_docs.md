# Sizer MCP Tool Documentation

Use the API-backed `size_position` tool for read-only sizing. Required critical inputs are bankroll, edge estimate, and either stop/structural max loss when relevant. Ask the user instead of guessing. State mutation tools require an explicit confirmation token.

Examples: dice exact math with book limit; futures stop trade; prediction contract with market price and hold-to-resolution; premium seller with stressed tail outcomes.
